<!-- ---
const images = [
  "public/Images/Imagen1.jpg",
  "public/Images/Imagen2.jpg",
  "public/Images/Imagen3.jpg",
  "public/Images/Imagen4.jpg",
  "public/Images/Imagen5.jpg",
  "public/Images/Imagen6.jpg",
];

const leftArrow = "public/Images/leftarrow.svg";
const rightArrow = "public/Images/rightarrow.svg";

let currentSlide = 1;

function handleNext() {
  currentSlide = (currentSlide === images.length) ? 1 : currentSlide + 1;
  updateSlides();
}

function handlePrev() {
  currentSlide = (currentSlide === 1) ? images.length : currentSlide - 1;
  updateSlides();
}

function updateSlides() {
  const slides = document.querySelectorAll(".slider__body");

  slides.forEach((slide, index) => {
    slide.classList.toggle("slider__body--show", index + 1 === currentSlide);
  });
}

window.onload = () => {
  const nextButton = document.getElementById("next");
  const prevButton = document.getElementById("before");

  nextButton.addEventListener("click", handleNext);
  prevButton.addEventListener("click", handlePrev);

  updateSlides();
};

---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Slider</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <section class="slider">
    <div class="slider__container container">
      <button onClick="handlePrev()" class="slider__arrow" id="before">
        <img src="public/Images/leftarrow.svg" alt="Left Arrow">
      </button>

      <div class="slider__slides">
        </div>

      <button onClick="handleNext()" class="slider__arrow" id="next">
        <img src="public/Images/rightarrow.svg" alt="Right Arrow">
      </button>
    </div>
  </section>

  <script src="slider.js"></script>
</body>
</html> -->
